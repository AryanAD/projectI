import { CLIENTS_URL } from "../constants";
import { apiSlice } from "./apiSlice";

export const clientApiSlice = apiSlice.injectEndpoints({
  endpoints: (builder) => ({}),
});

export const {} = clientApiSlice;
