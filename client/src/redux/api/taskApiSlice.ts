import { TASKS_URL } from "../constants";
import { apiSlice } from "./apiSlice";

export const taskApiSlice = apiSlice.injectEndpoints({
  endpoints: (builder) => ({}),
});

export const {} = taskApiSlice;
